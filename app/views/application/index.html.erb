<div id = "Main Container">
	<div id = "home_container">
		<h3> Main Page </h3>
				
		<p> List Gambar</p>
		<% @image.each do |f|%>
			<%= image_tag(f.prod_img_url(:standard).to_s)%>
		<%end%>

	</div>
</div>