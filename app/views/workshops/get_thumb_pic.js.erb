$('#uploaded_workshop_pic_box').html("<%= escape_javascript(render(:partial=>'showimage')) %>")

//this .js.erb to call ajax need to be the same as the controller action(method) that we called at the view page