<div id = "universal_form">
	<!--multiplat is true for uploading file-->
<%= form_for @inventory, html: {multipart: true} do |t| %>
	<% if @inventory.errors.any? %>
		<h2>Errors:</h2>
		<ul>
			<% @inventory.errors.full_messages.each do |message| %>
				<li><%= message %></li>
			<% end %>
		</ul>
	<%end%>	
	<p>	
		<%= t.label :Name %>
		<%= t.text_field :name%>
		<br />
		<%= t.label :Quantity %>
		<%= t.text_field :quantity%>
		<br />
		<%= t.label :Meter %>
		<%= t.text_field :meter%>
		<br />
		<%= t.label :Weight %>
		<%= t.text_field :weight%>
		<br />
		<%= t.label :SellPrice %>
		<%= t.text_field :sellprice%>
		<br />
		<%= t.label :Material %>
		<%= t.text_field :material%>
		<br />
		<%= t.label :Category %>
		<%= t.collection_select :category, @categories, :name, :name, :prompt=>true %>
		<br />
		<%= t.label :Type %>
		<%= t.collection_select :fabrictype, @type, :name, :name, :prompt=>true %>
		<br />
		<%= t.label :Link %>
		<%= t.text_field :link%>
		<br />
		<% if @inventory.id  %><!--kalau new berarti inventory.id belum ada-->
		<div id = "list_pic">
			<%= t.fields_for :product_images do |x| %>
				<%= image_tag(x.object.prod_img_url(:medium_thumb).to_s)%>
				<%= x.check_box :remove_prod_img %>
			<%end%>
			<fieldset>

					<% @inventory.product_images.each do |f|%>
						<%= image_tag(f.prod_img_url(:medium_thumb).to_s)%>

						ini id!!!<%= f.id %>

						<%= link_to 'remove', remove_photo_inventory_path(@inventory, :prod_id => f.id), :confirm => "Buang Gambar?", :method => :delete %>
					<% end%>
				
			</fieldset>
		</div>
		<% end %>
		
			<div id = "submit_button">
				Input Gambar Produk:
					<ul>
						<%= t.fields_for @product_images do |qty| %>                           
			                               <!--Where we upload multiple picture using carrierwave-->
			                               <li id = "picture"><%= qty.file_field :prod_img, multiple: true, name: "inventory[product_images_attributes][][prod_img]" %><a href ="#" id ="del_pic">x</a>|<a  href="#" id = "add_more_pic">More Pic</a></li><!--call paperclip upload file-->
			            <%end%> 
			        </ul>  
	        <%= t.submit "Submit" %> <!--this submit button will go to controller create action-->
	        </div>
		<br />		
		</div>
	</p>	
<%end%>

